@using System.Linq
@model Fashion_Store_System.Models.Supplier
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
    <style>
        body {
            background: #F8EDED;
            padding: 50px;
        }

        .form-card {
            background: white;
            border-radius: 25px;
            padding: 40px;
            max-width: 600px;
            margin: auto;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
        }

        .form-control {
            border-radius: 10px;
            padding: 12px;
            border: 1px solid #eee;
        }

        .btn-save {
            background: #40415A;
            color: white;
            border-radius: 50px;
            width: 100%;
            padding: 12px;
            font-weight: bold;
            border: none;
        }
    </style>
</head>
<body>
    <div class="form-card">
        <h3 class="text-center fw-bold mb-4" style="color: #40415A;">إضافة مورد جديد</h3>
        <form asp-action="Create">
            <div class="mb-3">
                <label class="form-label fw-bold">اسم المورد / المصنع</label>
                <input asp-for="Name" class="form-control" placeholder="مثلاً: مصنع النور للملابس" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label class="form-label fw-bold">رقم الهاتف</label>
                <input asp-for="Phone" class="form-control" placeholder="01xxxxxxxxx" />
            </div>
            <div class="mb-3">
                <label class="form-label fw-bold">العنوان</label>
                <textarea asp-for="Address" class="form-control" rows="2"></textarea>
            </div>
            <button type="submit" class="btn btn-save shadow mt-3">حفظ المورد</button>
            <a asp-action="Index" class="btn btn-link w-100 mt-2 text-decoration-none text-muted">العودة للقائمة</a>
        </form>
    </div>
</body>
</html>